eb is machine code for JMP, an unconditional jump. It is a two-byte opcode: follow it with the number of instruction bytes to jump.
74 is machine code for JE, jump if equal (zero flag set). Also a two-byte opcode, which can be replaced by an unconditional jump.
90 is machine code for NOP, a no operation instruction.
